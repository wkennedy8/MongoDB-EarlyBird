import React from 'react';
// 1. Get the id of the user from the params, and save it to a variable called "id"
// 2. When the component first mounts, using the id, make a call to our database to retrieve the user information.

const User = () => {
  return (
    <div>
      <h1>User Page</h1>
    </div>
  );
};

export default User;
